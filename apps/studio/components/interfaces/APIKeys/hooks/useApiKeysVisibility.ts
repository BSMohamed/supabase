// Stub file: APIKeys was removed in cleanup
// This file provides hooks to prevent build errors

export const useApiKeysVisibility = () => {
  return {
    isVisible: false,
    setIsVisible: () => {},
  }
}
