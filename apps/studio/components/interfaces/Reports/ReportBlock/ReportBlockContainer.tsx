// Stub file: Reports was removed in cleanup
// This component provides a placeholder to prevent build errors

import React from 'react'

export const ReportBlockContainer: React.FC<{ children?: React.ReactNode; [key: string]: any }> = ({
  children,
}) => {
  return <div className="border border-default rounded-lg p-4">{children}</div>
}
