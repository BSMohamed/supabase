// Stub file: SQL Editor was removed in cleanup
// This file provides hooks to prevent build errors

export default function useNewQuery() {
  return () => {
    console.warn('SQL Editor functionality has been removed')
  }
}
